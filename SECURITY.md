# Security

The bundled scripts execute local CLI/API calls and write benchmark outputs to disk. Review scripts before running them in sensitive environments.

Do not commit API keys, credentials, private prompts, confidential targets, or production system prompts.

To report a security issue, use the repository owner's private security-reporting channel rather than opening a public issue containing exploit details or secrets.

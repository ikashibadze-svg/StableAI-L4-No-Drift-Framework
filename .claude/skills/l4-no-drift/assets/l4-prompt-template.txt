STABLEAI L4 EXACT-ATTRACTOR MODE.

S = schema:
Return one valid JSON object only.

C = constraints:
No markdown.
No explanation.
No code fences.
No extra text.
No paraphrase.
No synonyms.
No added keys.
No removed keys.
No key reorder.
No whitespace change.
No punctuation change.
No case change.
No value change.

R = repair:
Before final output, compare the candidate with TARGET.
If any character differs, repair it to TARGET.
If text exists before or after TARGET, remove it.
If code fences exist, remove them.

A = attractor:
TARGET is the only allowed survivor.

TARGET:
{{PASTE_EXACT_CANONICAL_JSON_HERE_SINGLE_LINE}}

V = verification:
Internally check exact identity before emitting. External verification is performed by the caller.

FINAL:
Return TARGET exactly and nothing else.

---------------------------------------------------------------
COMPACT VARIANT — use only after it independently passes the target battery:

Return ONLY valid JSON. Copy TARGET exactly.
No markdown, fences, explanation, paraphrase, synonym, key, case, order, whitespace, or punctuation change.
Verify exact identity; if different, repair.
TARGET is the only allowed output.

TARGET:
{{PASTE_EXACT_CANONICAL_JSON_HERE_SINGLE_LINE}}

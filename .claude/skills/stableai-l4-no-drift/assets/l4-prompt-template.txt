STABLEAI L4 — EXACT-ATTRACTOR MODE

S — Schema
Return the declared TARGET representation exactly.

C — Constraints
No markdown. No explanation. No code fences. No extra text.
No paraphrase. No synonyms. No added/removed keys.
No key reorder. No punctuation, case, whitespace, or value change.

R — Repair
Before final output, compare candidate with TARGET.
If any byte differs, discard the candidate and restore TARGET exactly.
Remove any text before or after TARGET.

A — Attractor
TARGET is the only allowed survivor.

V — Verification
When requested/available, verify byte equality and SHA-256 externally.

FINAL
Return TARGET exactly and nothing else.

---

COMPRESSED VARIANT (benchmark independently before use)

Return TARGET exactly and nothing else. No paraphrase, formatting, case, order, whitespace, punctuation, key, or value change. If candidate differs, repair to TARGET before output.
